{
  "name": "games-app",
  "version": "1.4.0",
  "description": "Web-based games collection with 60+ games",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug",
    "test:e2e:codegen": "playwright codegen",
    "test:all": "npm run test && npm run test:e2e",
    "lint": "eslint *.js js/*.js --ext .js",
    "lint:fix": "eslint *.js js/*.js --ext .js --fix",
    "electron:dev": "cd electron && npm start",
    "electron:build": "cd electron && npm run package",
    "electron:setup": "cd electron && npm install"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@vitest/coverage-v8": "^1.0.0",
    "@vitest/ui": "^1.0.0"
  },
  "keywords": [
    "games",
    "chess",
    "puzzles",
    "web"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}